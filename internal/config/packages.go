package config

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/pelletier/go-toml/v2"
)

// PackagesConfig represents the user-defined packages configuration.
type PackagesConfig struct {
	Apt         AptConfig         `toml:"apt"`
	Brew        BrewConfig        `toml:"brew"`
	Bun         BunConfig         `toml:"bun"`
	Cargo       CargoConfig       `toml:"cargo"`
	Npm         NpmConfig         `toml:"npm"`
	Pi          PiConfig          `toml:"pi"`
	Pip         PipConfig         `toml:"pip"`
	Gems        GemsConfig        `toml:"gems"`
	PostInstall PostInstallConfig `toml:"post_install"`
	Tools       ToolsConfig       `toml:"tools"`
}

// AptConfig holds APT package settings.
type AptConfig struct {
	Packages []string `toml:"packages"`
}

// BrewConfig holds Homebrew package and tap settings.
type BrewConfig struct {
	Taps     []string `toml:"taps"`
	Packages []string `toml:"packages"`
}

// BunConfig holds Bun package settings.
type BunConfig struct {
	Packages []string `toml:"packages"`
}

// CargoConfig holds Cargo package settings.
type CargoConfig struct {
	Packages []string `toml:"packages"`
}

// NpmConfig holds NPM package settings.
type NpmConfig struct {
	Packages    []string `toml:"packages"`
	PostInstall []string `toml:"post_install"`
}

// PipConfig holds PIP package settings.
type PipConfig struct {
	Packages []string `toml:"packages"`
}

// PiConfig holds Pi extension package sources. Unlike [npm] (which installs
// globally), Pi resolves extensions from its own local tree at
// ~/.pi/agent/npm. Sources use pi's install syntax: npm:name,
// git:github.com/user/repo, url, or a local path. The generated script drives
// `pi install`, which is idempotent and manages settings.json atomically.
type PiConfig struct {
	Packages []string `toml:"packages"`
}

// GemsConfig holds Ruby gem package settings.
type GemsConfig struct {
	Packages []string `toml:"packages"`
}

// PostInstallConfig holds post-install commands.
type PostInstallConfig struct {
	Commands []string `toml:"commands"`
}

// ToolsConfig holds specialized tool toggles.
type ToolsConfig struct {
	PhpBrew bool `toml:"phpbrew"`
	Nix     bool `toml:"nix"`
	Nvm     bool `toml:"nvm"`
	Vmr     bool `toml:"vmr"`
	Asdf    bool `toml:"asdf"`
	Mise    bool `toml:"mise"`
	Volta   bool `toml:"volta"`
	Bun     bool `toml:"bun"` // Deprecated: Bun is always installed.
}

// LoadPackages reads the packages.toml file.
func LoadPackages() (*PackagesConfig, error) {
	configPath := filepath.Join(GetConfigDir(), "packages.toml")

	if _, err := os.Stat(configPath); os.IsNotExist(err) {
		return &PackagesConfig{}, nil
	}

	data, err := os.ReadFile(configPath)
	if err != nil {
		return nil, fmt.Errorf("failed to read packages.toml: %w", err)
	}

	var config PackagesConfig
	if err := toml.Unmarshal(data, &config); err != nil {
		return nil, fmt.Errorf("failed to parse packages.toml: %w", err)
	}

	return &config, nil
}

// SavePackages writes the packages config back to packages.toml.
func (c *PackagesConfig) SavePackages() error {
	configPath := filepath.Join(GetConfigDir(), "packages.toml")

	data, err := toml.Marshal(c)
	if err != nil {
		return fmt.Errorf("failed to marshal packages config: %w", err)
	}

	if err := os.WriteFile(configPath, data, 0644); err != nil {
		return fmt.Errorf("failed to write packages.toml: %w", err)
	}

	return nil
}

// GenerateInstallScript generates a bash script to install configured packages.
func (c *PackagesConfig) GenerateInstallScript() string {
	var b strings.Builder
	b.WriteString("#!/bin/bash\n# Generated by Construct CLI - DO NOT EDIT\n\nset -e\n\n")

	// Clear patching marker to ensure re-patching after installs
	b.WriteString("rm -f \"$HOME/.construct_patched\"\n\n")

	// Sudo helper: detect if we need sudo and if it's functional
	b.WriteString("# Sudo detection: skip if root, test if sudo works, otherwise skip privileged ops\n")
	b.WriteString("SUDO_AVAILABLE=1\n")
	b.WriteString("if [ \"$(id -u)\" = \"0\" ]; then\n")
	b.WriteString("    SUDO=\"\"\n")
	b.WriteString("elif sudo -n apt-get --version &>/dev/null; then\n")
	b.WriteString("    SUDO=\"sudo\"\n")
	b.WriteString("else\n")
	b.WriteString("    echo '⚠️  sudo not available or not configured - skipping privileged package operations'\n")
	b.WriteString("    SUDO_AVAILABLE=0\n")
	b.WriteString("fi\n\n")

	// Diagnostics (helps troubleshoot UID/GID, PATH and package manager issues)
	b.WriteString("echo '=== Construct setup diagnostics ==='\n")
	b.WriteString("echo \"Timestamp (UTC): $(date -u +%Y-%m-%dT%H:%M:%SZ)\"\n")
	b.WriteString("echo \"User IDs: uid=$(id -u) gid=$(id -g)\"\n")
	b.WriteString("echo \"User names: user=$(id -un 2>/dev/null || echo unknown) group=$(id -gn 2>/dev/null || echo unknown)\"\n")
	b.WriteString("echo \"HOME: $HOME\"\n")
	b.WriteString("echo \"SHELL: ${SHELL:-unknown}\"\n")
	b.WriteString("echo \"PATH: $PATH\"\n")
	b.WriteString("if [ -d /home/linuxbrew/.linuxbrew ]; then\n")
	b.WriteString("    if [ -w /home/linuxbrew/.linuxbrew ]; then\n")
	b.WriteString("        echo \"Homebrew dir writable: /home/linuxbrew/.linuxbrew\"\n")
	b.WriteString("    else\n")
	b.WriteString("        echo \"⚠️ Homebrew dir not writable by current user: /home/linuxbrew/.linuxbrew\"\n")
	b.WriteString("        ls -ld /home/linuxbrew/.linuxbrew 2>/dev/null || true\n")
	b.WriteString("    fi\n")
	b.WriteString("fi\n")
	b.WriteString("if command -v brew &> /dev/null; then\n")
	b.WriteString("    echo \"brew: $(command -v brew)\"\n")
	b.WriteString("    brew --version | head -1 || true\n")
	b.WriteString("else\n")
	b.WriteString("    echo \"brew: not found\"\n")
	b.WriteString("fi\n")
	b.WriteString("if command -v npm &> /dev/null; then\n")
	b.WriteString("    echo \"npm: $(command -v npm)\"\n")
	b.WriteString("    npm --version || true\n")
	b.WriteString("else\n")
	b.WriteString("    echo \"npm: not found\"\n")
	b.WriteString("fi\n")
	b.WriteString("echo '=================================='\n\n")

	// Critical Packages (Safety check - ensure Dockerfile packages are present)
	b.WriteString("# Critical Packages (only if sudo available)\n")
	b.WriteString("if [ \"$SUDO_AVAILABLE\" = \"1\" ]; then\n")
	b.WriteString("    echo 'Verifying critical packages...'\n")
	b.WriteString("    $SUDO apt-get update\n")
	b.WriteString("    $SUDO apt-get install -y build-essential git curl wget sudo gosu socat bubblewrap xvfb\n")
	b.WriteString("fi\n\n")

	// Core APT Packages (Always installed, only if sudo available)
	b.WriteString("if [ \"$SUDO_AVAILABLE\" = \"1\" ]; then\n")
	b.WriteString("    echo 'Installing core system packages...'\n")
	b.WriteString("    $SUDO apt-get install -y procps file nano python3 python3-pip python3-venv python3-dev pipx ufw iptables dnsutils xclip xsel wl-clipboard\n")
	b.WriteString("fi\n\n")

	// Initialize Homebrew environment
	b.WriteString("# Initialize Homebrew environment\n")
	b.WriteString("if [ -f /home/linuxbrew/.linuxbrew/bin/brew ]; then\n")
	b.WriteString("    eval \"$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)\"\n")
	b.WriteString("fi\n\n")

	// Configure npm before global installs to avoid EACCES on /usr/local with non-root UIDs
	b.WriteString("echo 'Configuring npm global prefix...'\n")
	b.WriteString("if command -v npm &> /dev/null; then\n")
	b.WriteString("    mkdir -p \"$HOME/.npm-global\"\n")
	b.WriteString("    npm config set prefix \"$HOME/.npm-global\" || echo \"⚠️ Failed to set npm prefix\"\n")
	b.WriteString("    export PATH=\"$HOME/.npm-global/bin:$PATH\"\n")
	b.WriteString("    echo \"npm global prefix: $(npm config get prefix 2>/dev/null || echo unknown)\"\n")
	b.WriteString("else\n")
	b.WriteString("    echo \"⚠️ npm not found; skipping npm prefix configuration\"\n")
	b.WriteString("fi\n\n")

	b.WriteString("echo 'Installing Bun...'\n")
	b.WriteString("export BUN_INSTALL=\"$HOME/.bun\"\n")
	b.WriteString("export PATH=\"$BUN_INSTALL/bin:$PATH\"\n")
	b.WriteString("if command -v bun &> /dev/null; then\n")
	b.WriteString("    echo \"Bun already installed; skipping.\"\n")
	b.WriteString("else\n")
	b.WriteString("    curl -fsSL https://bun.sh/install | bash\n")
	b.WriteString("fi\n\n")

	// Standard Tools (Always installed)
	b.WriteString("echo 'Installing Amp CLI...'\n")

	b.WriteString("if [ -x \"/home/construct/.local/bin/amp\" ] || [ -x \"/home/construct/.amp/bin/amp\" ]; then\n")
	b.WriteString("    echo \"Amp already installed; skipping.\"\n")
	b.WriteString("else\n")
	b.WriteString("    curl -fsSL https://ampcode.com/install.sh | bash\n")
	b.WriteString("fi\n\n")

	b.WriteString("if command -v brew &> /dev/null; then\n")
	b.WriteString("    INSTALLED_BREW=$(brew list --formula -1 2>/dev/null || true)\n")

	b.WriteString("    echo 'Installing imagemagick (required for clipboard)...'\n")
	b.WriteString("    if echo \"$INSTALLED_BREW\" | grep -q \"^imagemagick$\"; then\n")
	b.WriteString("        echo \"  ✓ imagemagick already installed\"\n")
	b.WriteString("    else\n")
	b.WriteString("        brew install --formula imagemagick || echo \"⚠️ Failed to install imagemagick\"\n")
	b.WriteString("    fi\n\n")

	b.WriteString("    echo 'Installing topgrade (system updater)...'\n")
	b.WriteString("    if echo \"$INSTALLED_BREW\" | grep -q \"^topgrade$\"; then\n")
	b.WriteString("        echo \"  ✓ topgrade already installed\"\n")
	b.WriteString("    else\n")
	b.WriteString("        brew install --formula topgrade || echo \"⚠️ Failed to install topgrade\"\n")
	b.WriteString("    fi\n\n")

	b.WriteString("    echo 'Installing libgit2 (dependency for cargo-update)...'\n")
	b.WriteString("    if echo \"$INSTALLED_BREW\" | grep -q \"^libgit2$\"; then\n")
	b.WriteString("        echo \"  ✓ libgit2 already installed\"\n")
	b.WriteString("    else\n")
	b.WriteString("        brew install --formula libgit2 || echo \"Warning: libgit2 installation failed\"\n")
	b.WriteString("    fi\n")
	b.WriteString("else\n")
	b.WriteString("    echo \"⚠️ Homebrew not found; skipping core brew packages\"\n")
	b.WriteString("fi\n\n")

	b.WriteString("echo 'Installing cargo-update (enables cargo package updates)...'\n")
	b.WriteString("if command -v cargo &> /dev/null; then\n")
	b.WriteString("    cargo install cargo-update 2>/dev/null || echo \"Warning: cargo-update installation failed, cargo package updates disabled\"\n")
	b.WriteString("fi\n\n")

	// Specialized Tools (Priority)
	if c.Tools.PhpBrew {
		b.WriteString("echo 'Installing PHPBrew...'\n")
		b.WriteString("mkdir -p $HOME/.local/bin\n")
		b.WriteString("curl -L -O https://github.com/phpbrew/phpbrew/releases/latest/download/phpbrew.phar\n")
		b.WriteString("chmod +x phpbrew.phar\n")
		b.WriteString("mv phpbrew.phar $HOME/.local/bin/phpbrew\n")
		b.WriteString("phpbrew init\n")
		b.WriteString("phpbrew lookup-prefix homebrew\n\n")
	}

	if c.Tools.Nix {
		b.WriteString("echo 'Installing Nix...'\n")
		b.WriteString("sh <(curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install) --no-daemon\n\n")
	}

	if c.Tools.Nvm {
		b.WriteString("echo 'Installing NVM...'\n")
		b.WriteString("curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash\n\n")
	}

	if c.Tools.Vmr {
		b.WriteString("echo 'Installing VMR...'\n")
		b.WriteString("curl --proto '=https' --tlsv1.2 -sSf https://scripts.vmr.dpdns.org | sh\n\n")
	}

	if c.Tools.Asdf {
		b.WriteString("echo 'Installing asdf via Homebrew...'\n")
		b.WriteString("if command -v brew &> /dev/null; then\n")
		b.WriteString("    brew install asdf || echo \"⚠️ Failed to install asdf\"\n")
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ Homebrew not found; skipping asdf\"\n")
		b.WriteString("fi\n\n")
	}

	if c.Tools.Mise {
		b.WriteString("echo 'Installing mise...'\n")
		b.WriteString("curl https://mise.run | sh\n\n")
	}

	if c.Tools.Volta {
		b.WriteString("echo 'Installing Volta...'\n")
		b.WriteString("curl https://get.volta.sh | bash -s -- --skip-setup\n\n")
	}

	// APT (only if sudo available)
	if len(c.Apt.Packages) > 0 {
		b.WriteString("if [ \"$SUDO_AVAILABLE\" = \"1\" ]; then\n")
		b.WriteString("    echo 'Installing APT packages...'\n")
		b.WriteString("    $SUDO apt-get update\n")
		b.WriteString("    $SUDO apt-get install -y ")
		for _, pkg := range c.Apt.Packages {
			b.WriteString(pkg + " ")
		}
		b.WriteString("\n")
		b.WriteString("fi\n\n")
	}

	// Brew
	if len(c.Brew.Taps) > 0 || len(c.Brew.Packages) > 0 {
		b.WriteString("echo 'Installing Homebrew packages...'\n")
		b.WriteString("if command -v brew &> /dev/null; then\n")
		for _, tap := range c.Brew.Taps {
			b.WriteString("    if ! brew tap " + tap + "; then\n")
			b.WriteString("        echo \"⚠️ Failed to tap " + tap + "\"\n")
			b.WriteString("    fi\n")
		}
		if len(c.Brew.Packages) > 0 {
			b.WriteString("    # Get list of installed formulae to avoid noise\n")
			b.WriteString("    INSTALLED_BREW=$(brew list --formula -1 2>/dev/null || true)\n")
			for _, pkg := range c.Brew.Packages {
				// Use --formula to avoid disambiguation errors with tap-qualified names
				b.WriteString("    if echo \"$INSTALLED_BREW\" | grep -q \"^" + pkg + "$\"; then\n")
				b.WriteString("        echo \"  ✓ " + pkg + " already installed\"\n")
				b.WriteString("    else\n")
				b.WriteString("        if ! brew install --formula " + pkg + "; then\n")
				b.WriteString("            echo \"⚠️ Failed to install " + pkg + "\"\n")
				b.WriteString("        fi\n")
				b.WriteString("    fi\n")
			}
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ Homebrew not found; skipping Homebrew packages\"\n")
		b.WriteString("fi\n")
		b.WriteString("\n")
	}

	// Cargo
	if len(c.Cargo.Packages) > 0 {
		b.WriteString("echo 'Installing Cargo packages...'\n")
		b.WriteString("# Ensure cargo is in PATH\n")
		b.WriteString("if command -v cargo &> /dev/null; then\n")
		for _, pkg := range c.Cargo.Packages {
			b.WriteString("    cargo install " + pkg + " || echo \"⚠️ Failed to install " + pkg + "\"\n")
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ Cargo not found; skipping Rust packages\"\n")
		b.WriteString("fi\n\n")
	}

	// Bun
	if len(c.Bun.Packages) > 0 {
		b.WriteString("echo 'Installing Bun packages...'\n")
		b.WriteString("if command -v bun &> /dev/null; then\n")
		for _, pkg := range c.Bun.Packages {
			b.WriteString("    bun install -g " + pkg + " || echo \"⚠️ Failed to install " + pkg + "\"\n")
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ Bun not found; skipping Bun packages\"\n")
		b.WriteString("fi\n\n")
	}

	// NPM
	if len(c.Npm.Packages) > 0 {
		b.WriteString("echo 'Installing NPM packages...'\n")
		for _, pkg := range c.Npm.Packages {
			b.WriteString("npm install -g " + pkg + " || echo \"⚠️ Failed to install " + pkg + "\"\n")
		}
		b.WriteString("\n\n")
	}
	if len(c.Npm.PostInstall) > 0 {
		b.WriteString("echo 'Running NPM post-install commands...'\n")
		b.WriteString("export NPM_CONFIG_YES=true\n")
		for _, cmd := range c.Npm.PostInstall {
			b.WriteString(cmd + "\n")
		}
		b.WriteString("\n")
	}

	// Pi extensions (via pi's own installer; manages ~/.pi state atomically).
	// Runs after [npm] so `pi` exists on PATH. pi install is idempotent.
	if len(c.Pi.Packages) > 0 {
		b.WriteString("echo 'Installing Pi extension packages...'\n")
		b.WriteString("if command -v pi &> /dev/null; then\n")
		for _, pkg := range c.Pi.Packages {
			b.WriteString("    pi install " + pkg + " || echo \"⚠️ Failed to install Pi package: " + pkg + "\"\n")
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ pi not found; skipping Pi packages\"\n")
		b.WriteString("fi\n\n")
	}

	// PIP (using pipx for PEP 668 compliance)
	if len(c.Pip.Packages) > 0 {
		b.WriteString("echo 'Installing PIP packages via pipx...'\n")
		b.WriteString("# Ensure pipx PATH is configured\n")
		b.WriteString("export PATH=\"$HOME/.local/bin:$PATH\"\n")
		b.WriteString("if command -v pipx &> /dev/null; then\n")
		for _, pkg := range c.Pip.Packages {
			b.WriteString("    pipx install " + pkg + " || echo \"⚠️ Failed to install " + pkg + "\"\n")
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ pipx not found; skipping Python packages\"\n")
		b.WriteString("fi\n\n")
	}

	// Gems
	if len(c.Gems.Packages) > 0 {
		b.WriteString("echo 'Installing Ruby gems...'\n")
		b.WriteString("# Ensure gem is in PATH\n")
		b.WriteString("if command -v gem &> /dev/null; then\n")
		for _, pkg := range c.Gems.Packages {
			b.WriteString("    gem install " + pkg + " || echo \"⚠️ Failed to install " + pkg + "\"\n")
		}
		b.WriteString("else\n")
		b.WriteString("    echo \"⚠️ Gem not found; skipping Ruby packages\"\n")
		b.WriteString("fi\n\n")
	}

	// Post-install
	if len(c.PostInstall.Commands) > 0 {
		b.WriteString("echo 'Running post-install commands...'\n")
		b.WriteString("export NPM_CONFIG_YES=true\n")
		for _, cmd := range c.PostInstall.Commands {
			// Auto-migrate legacy or incompatible commands
			switch cmd {
			case "agent-browser install --with-deps":
				cmd = "if [ \"$(uname -m)\" = \"aarch64\" ] || [ \"$(uname -m)\" = \"arm64\" ]; then echo \"ℹ️ ARM64 detected: Configuring agent-browser to use system Chromium\"; mkdir -p ~/.local/bin; printf '#!/bin/bash\\nexec \"'\"$(npm config get prefix)\"'\"/bin/agent-browser --executable-path /usr/bin/chromium \"$@\"' > ~/.local/bin/agent-browser && chmod +x ~/.local/bin/agent-browser; else agent-browser install --with-deps; fi"
			case "if [ -x \"$HOME/.opencode/bin/opencode\" ]; then echo \"OpenCode already installed\"; else curl -fsSL https://opencode.ai/install | bash; fi":
				cmd = "curl -fsSL https://opencode.ai/install | bash"
			}

			b.WriteString(cmd + " || echo \"⚠️ Post-install command failed: " + cmd + "\"\n")
		}
		b.WriteString("\n")
	}

	b.WriteString("echo 'Post-install command verification...'\n")
	b.WriteString("missing_cmds=\"\"\n")
	b.WriteString("for cmd in claude amp copilot opencode qwen cline crush codex goose agy kilocode pi; do\n")
	b.WriteString("    if command -v \"$cmd\" &> /dev/null; then\n")
	b.WriteString("        cmd_path=$(command -v \"$cmd\")\n")
	b.WriteString("        echo \"  ✓ $cmd -> $cmd_path\"\n")
	b.WriteString("    else\n")
	b.WriteString("        echo \"  - $cmd not found in PATH\"\n")
	b.WriteString("        missing_cmds=\"$missing_cmds $cmd\"\n")
	b.WriteString("    fi\n")
	b.WriteString("done\n\n")
	b.WriteString("if [ -n \"$missing_cmds\" ]; then\n")
	b.WriteString("    echo \"⚠️ Missing commands after setup:$missing_cmds\"\n")
	b.WriteString("    echo \"   If these agents are expected, re-run: construct sys packages --install\"\n")
	b.WriteString("fi\n\n")

	b.WriteString("echo 'User package installation completed successfully.'\n")
	return b.String()
}

// GenerateTopgradeConfig generates a topgrade.toml configuration based on enabled tools.
func (c *PackagesConfig) GenerateTopgradeConfig() string {
	var disabledSteps []string
	var ignoreFailures []string

	baseDisabled := []string{
		"firmware",
		"flatpak",
		"snap",
		"containers",
		"claude_code",
		"pnpm",
		"sdkman",
		"vagrant",
		"wsl",
		"lensfun",
		"vim",
		"emacs",
		"vscode",
		"atom",
		"git_repos",
		"gnome_shell_extensions",
		"rcm",
		"remotes",
		"restarts",
		"shell",
		"sparkle",
		"tmux",
		"toolbx",
	}
	disabledSteps = append(disabledSteps, baseDisabled...)

	if !c.Tools.Nix {
		disabledSteps = append(disabledSteps, "nix")
	}
	// Always disable topgrade's node step: npm update -g doesn't cross semver
	// boundaries, so we handle npm global upgrades in update-all.sh instead.
	disabledSteps = append(disabledSteps, "node")
	if !c.Tools.Asdf {
		disabledSteps = append(disabledSteps, "asdf")
	}
	if !c.Tools.Mise {
		disabledSteps = append(disabledSteps, "mise")
	} else {
		// mise may fail on transient GitHub API 403/rate-limit errors; don't fail the whole update run.
		ignoreFailures = append(ignoreFailures, "mise")
	}
	if !c.Tools.Volta {
		disabledSteps = append(disabledSteps, "volta_packages")
	}
	// Always disable pip3 (PEP 668 compliance), only enable pipx when pip packages exist
	disabledSteps = append(disabledSteps, "pip3")
	if len(c.Pip.Packages) == 0 {
		disabledSteps = append(disabledSteps, "pipx")
	}
	if len(c.Gems.Packages) == 0 {
		disabledSteps = append(disabledSteps, "gem")
	}

	var b strings.Builder
	b.WriteString("[misc]\n")
	b.WriteString("assume_yes = true\n")
	b.WriteString("no_retry = true\n")
	b.WriteString("no_self_update = true\n")
	b.WriteString("pre_sudo = true\n")
	b.WriteString("show_skipped = false\n")
	b.WriteString("display_time = true\n")
	if len(ignoreFailures) > 0 {
		b.WriteString("ignore_failures = [\n")
		for _, step := range ignoreFailures {
			fmt.Fprintf(&b, "    %q,\n", step)
		}
		b.WriteString("]\n")
	}
	b.WriteString("disable = [\n")
	for _, step := range disabledSteps {
		fmt.Fprintf(&b, "    %q,\n", step)
	}
	b.WriteString("]\n\n")

	b.WriteString("[brew]\n")
	b.WriteString("autoremove = true\n")
	b.WriteString("greedy_cask = true\n\n")

	b.WriteString("[npm]\n")
	b.WriteString("use_sudo = false\n\n")

	b.WriteString("[yarn]\n")
	b.WriteString("use_sudo = false\n\n")

	b.WriteString("[git]\n")
	b.WriteString("pull_predefined = false\n\n")

	b.WriteString("[composer]\n")
	b.WriteString("self_update = false\n\n")

	b.WriteString("[commands]\n")
	b.WriteString("\"Claude Code\" = \"if command -v claude &> /dev/null; then claude update || true; fi\"\n")
	b.WriteString("\"Pi Coding Agent\" = \"if command -v pi &> /dev/null; then pi update --all || true; fi\"\n")
	b.WriteString("\"Antigravity Agent\" = \"if command -v agy &> /dev/null; then agy update || true; fi\"\n")

	return b.String()
}
